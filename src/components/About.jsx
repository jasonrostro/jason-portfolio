import React from 'react';
import { motion } from 'framer-motion';
import { styles } from '../styles';
import { services } from '../constants';
import { fadeIn, textVariant } from '../utils/motion';
import { SectionWrapper } from '../hoc';

const ServiceCard = ({ index, title, icon }) => {
  return (
    <motion.div
      variants={fadeIn('right', 'spring', 0.5 * index, 0.75)}
      className="xs:w-[250px] w-full card-gradient p-[1px] rounded-[20px] shadow-card">
      <div
        options={{
          max: 45,
          scale: 1,
          speed: 450,
        }}
        className="bg-jetLight rounded-[20px] py-5 px-12 min-h-[280px] flex justify-evenly items-center flex-col">
        <img src={icon} alt={title} className="w-16 h-16 object-contain" />
        <h3 className="text-taupe text-[18px] font-bold text-center">
          {title}
        </h3>
      </div>
    </motion.div>
  );
};

const About = () => {
  return (
    <div className="-mt-[6rem]">
      <motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Introduction</p>
        <h2 className={styles.sectionHeadText}>Overview.</h2>
      </motion.div>

      <motion.p
        variants={fadeIn('', '', 0.1, 1)}
        className="mt-4 text-taupe text-[18px] max-w-3xl leading-[30px]">
        I fulfill my destiny based on a life philosophy: “Be a lifelong learner,” “Follow your passions,” and “Stay hungry,
        stay foolish.”
        I am a versatile full-stack engineer with comprehensive experience in product development from inception to
        completion. My expertise includes code enhancement and refactoring, creating SaaS solutions, managing legacy
        codebases, and handling tech migration and decommissioning projects in complex, demanding, and large-scale
        enterprises with high volumes of data.
        I excel in troubleshooting complex client-side issues and creating efficient, maintainable code that scales with the
        company's requirements. I am passionate about staying up-to-date with the latest trends in the field and have a knack
        for quickly learning new technologies.
        As a proactive team player, I thrive in collaborating with cross-functional teams of all sizes. I have expertise in
        JavaScript, TypeScript, Python, PHP, Golang, ASP.NET, and Elixir for both frontend and backend development.
      </motion.p>

      <div className="mt-20 flex flex-wrap gap-10">
        {services.map((service, index) => (
          <ServiceCard key={service.title} index={index} {...service} />
        ))}
      </div>
    </div>
  );
};

export default SectionWrapper(About, 'about');
